function obtenerAreaTriangulo(){
    var base = prompt("Ingresa la medida de la base del triángulo:");
    var altura = prompt("Ingresa la medida de la altura del triángulo:");
    var area = base*altura/2;
    alert(area);
}